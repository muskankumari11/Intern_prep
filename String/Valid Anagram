class Solution {
public:
    bool isAnagram(string s, string t) {
        int n=s.size();
        for(int i=0;i<n;i++){
            if(s.size()!=t.size())
            return false;
        }
        sort(s.begin(),s.end());
        sort(t.begin(),t.end());
        return s==t;
    }
};
